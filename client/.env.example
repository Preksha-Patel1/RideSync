# Base URL of the RideSync REST API (server/src/app.js mounts routes under /api)
VITE_API_URL=http://localhost:5050/api
# Base URL Socket.IO connects to (server/src/config/socket.js — same server, no /api suffix)
VITE_SOCKET_URL=http://localhost:5050
